/**
 * App overview page — placeholder for Prompt 6.
 * Renders inside the (app) layout shell so the sidebar/topbar are testable.
 */
export default function AppOverviewPage() {
    return (
        <div className="space-y-1">
            <h2 className="text-lg font-semibold tracking-tight">Overview</h2>
            <p className="text-sm text-muted-foreground">
                Your workspace at a glance. Content will be added in a future prompt.
            </p>
        </div>
    );
}
